{
    "org": {
        "sql": "SELECT notifications.* FROM notifications WHERE notifications.organization_id = \"$1\" AND notifications.user_id IS NULL ORDER BY notifications.notified_at DESC LIMIT \"$2\"",
        "cost": 8.32,
        "rewrite_types": []
    },
    "rewrites": [
        {
            "sql": "SELECT notifications.* FROM notifications WHERE notifications.organization_id = \"$1\" AND False ORDER BY notifications.notified_at DESC LIMIT \"$2\"",
            "cost": 0.02,
            "rewrite_types": [
                "RewriteNullPredicate"
            ]
        }
    ]
}