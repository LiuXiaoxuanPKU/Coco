{
    "org": {
        "sql": "SELECT DISTINCT collections.* FROM collections INNER JOIN articles ON articles.collection_id = collections.id WHERE collections.user_id = \"$1\" ORDER BY collections.created_at DESC",
        "cost": 24.73,
        "rewrite_types": []
    },
    "rewrites": [
        {
            "sql": "SELECT collections.* FROM collections INNER JOIN articles ON articles.collection_id = collections.id WHERE collections.user_id = \"$1\" ORDER BY collections.created_at DESC",
            "cost": 24.68,
            "rewrite_types": [
                "RemoveDistinct"
            ]
        }
    ]
}