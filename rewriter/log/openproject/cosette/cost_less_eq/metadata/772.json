{
    "org": {
        "sql": "SELECT members.* FROM members WHERE members.id = \"$1\" AND members.id NOT IN (SELECT DISTINCT member_roles.member_id FROM member_roles)",
        "cost": 343.59,
        "rewrite_types": []
    },
    "rewrites": [
        {
            "sql": "SELECT members.* FROM members WHERE members.id = \"$1\" AND members.id NOT IN (SELECT member_roles.member_id FROM member_roles)",
            "cost": 188.31,
            "rewrite_types": [
                "RemoveDistinct"
            ]
        }
    ]
}